
extern crate subset;
use subset::multi::*;

fn main() {
    // let mut set = vec![9, 8, 7, 6, 5, 4, 3, 2, 1, 0];
    // let idxs = vec![2, 4, 7];
    // let mut subset = SubsetMut::new(&mut set, &idxs).unwrap();
    // let mut iter = subset.iter();
    // let r2 = iter.next().unwrap();
    // let r3 = iter.next().unwrap();
    // *r2 = 19;
    // *r3 = 33;
    // // assert_eq!(set, vec![9, 8, 19, 6, 33, 4, 3, 2, 1, 0]);
    // let mut sum = 0;
    // for e in subset.iter().map(|v| 2* *v).rev() {
    //     sum += e;
    // }
    // //assert_eq!(sum, 108);
    // *r2 = 10;

}
